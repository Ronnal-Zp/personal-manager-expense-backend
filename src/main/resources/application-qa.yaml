spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/personal_manager_expense_qa}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: INFO
    com.aldahirzamora: INFO
