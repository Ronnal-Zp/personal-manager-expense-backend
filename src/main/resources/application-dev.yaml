spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/personal_manager_expense_dev}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.aldahirzamora: DEBUG
